[package]
name = "wasp"
version = "0.1.0"
edition = "2024"
description = "A WebAssembly runtime written in Rust, compliant with the 1.0 spec."

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
rand = "0.10.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies]

