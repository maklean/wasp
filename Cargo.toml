[package]
name = "wasp"
version = "0.1.0"
edition = "2024"
description = "A WebAssembly interpreter written in Rust, compliant with the 1.0 spec."

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies]

